<template><el-form label-width="130px"><el-form-item label="CRUD 能力"><el-checkbox v-for="key in keys" :key="key" v-model="model.capabilities[key]" :label="key">{{ key }}</el-checkbox></el-form-item><el-form-item label="启用数据范围"><el-switch v-model="model.dataScope.enabled" /></el-form-item><el-form-item v-if="model.dataScope.enabled" label="数据范围字段"><el-select v-model="model.dataScope.field" filterable><el-option v-for="field in model.fields" :key="field.name" :value="field.name" :label="field.name" /></el-select></el-form-item></el-form></template>
<script setup lang="ts">import type { CrudDefinition } from '@/types/development/crud'; defineProps<{ model: CrudDefinition }>(); const keys = ['list', 'search', 'form', 'detail', 'create', 'update', 'delete', 'import', 'export'] as const;</script>
